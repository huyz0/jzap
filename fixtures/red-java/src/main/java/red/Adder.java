package red;

/** Production code for a fixture whose test suite is deliberately already failing. */
public class Adder {

    public int add(int a, int b) {
        return a + b;
    }

    public int subtract(int a, int b) {
        return a - b;
    }
}
